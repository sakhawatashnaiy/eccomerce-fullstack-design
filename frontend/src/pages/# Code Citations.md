# Code Citations

## License: unknown
https://github.com/Marcelodr1999/ecommerceWeb/blob/fb65547b4e7859e2d9e04a6ce3455b579a7ac8de/src/components/Checkout.jsx

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [current
```


## License: unknown
https://github.com/Marcelodr1999/ecommerceWeb/blob/fb65547b4e7859e2d9e04a6ce3455b579a7ac8de/src/components/Checkout.jsx

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [current
```


## License: unknown
https://github.com/Marcelodr1999/ecommerceWeb/blob/fb65547b4e7859e2d9e04a6ce3455b579a7ac8de/src/components/Checkout.jsx

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [current
```


## License: unknown
https://github.com/Marcelodr1999/ecommerceWeb/blob/fb65547b4e7859e2d9e04a6ce3455b579a7ac8de/src/components/Checkout.jsx

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [current
```


## License: unknown
https://github.com/Amraaa/AmraDev1/blob/ebc5a427a4678b5cda4919a5ead9a4879dd55167/client/src/components/profile-forms/AddEducation.js

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    
```


## License: unknown
https://github.com/Amraaa/AmraDev1/blob/ebc5a427a4678b5cda4919a5ead9a4879dd55167/client/src/components/profile-forms/AddEducation.js

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    
```


## License: unknown
https://github.com/Amraaa/AmraDev1/blob/ebc5a427a4678b5cda4919a5ead9a4879dd55167/client/src/components/profile-forms/AddEducation.js

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    
```


## License: unknown
https://github.com/Amraaa/AmraDev1/blob/ebc5a427a4678b5cda4919a5ead9a4879dd55167/client/src/components/profile-forms/AddEducation.js

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    
```


## License: unknown
https://github.com/Amraaa/AmraDev1/blob/ebc5a427a4678b5cda4919a5ead9a4879dd55167/client/src/components/profile-forms/AddEducation.js

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    
```


## License: unknown
https://github.com/Amraaa/AmraDev1/blob/ebc5a427a4678b5cda4919a5ead9a4879dd55167/client/src/components/profile-forms/AddEducation.js

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    
```


## License: unknown
https://github.com/Amraaa/AmraDev1/blob/ebc5a427a4678b5cda4919a5ead9a4879dd55167/client/src/components/profile-forms/AddEducation.js

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    
```


## License: unknown
https://github.com/huangsihao7/scooter-WSVA/blob/e4c27bad7673a3dae733e279dbb6554eececd75e/frontend/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width
```


## License: MIT
https://github.com/persianpack/full-event-calendar/blob/9799660f3db5412224030fbf79303276e97061be/README.md

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width
```


## License: unknown
https://github.com/huangsihao7/scooter-WSVA/blob/e4c27bad7673a3dae733e279dbb6554eececd75e/frontend/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width
```


## License: MIT
https://github.com/persianpack/full-event-calendar/blob/9799660f3db5412224030fbf79303276e97061be/README.md

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width
```


## License: unknown
https://github.com/huangsihao7/scooter-WSVA/blob/e4c27bad7673a3dae733e279dbb6554eececd75e/frontend/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width
```


## License: MIT
https://github.com/persianpack/full-event-calendar/blob/9799660f3db5412224030fbf79303276e97061be/README.md

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width
```


## License: unknown
https://github.com/huangsihao7/scooter-WSVA/blob/e4c27bad7673a3dae733e279dbb6554eececd75e/frontend/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width
```


## License: MIT
https://github.com/persianpack/full-event-calendar/blob/9799660f3db5412224030fbf79303276e97061be/README.md

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width
```


## License: unknown
https://github.com/harish2704/harish2704.github.io/blob/c8e145dd64198845847e1f0d30a55fc9f700071b/online-group-vcf-file-generator/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;
```


## License: unknown
https://github.com/huangsihao7/scooter-WSVA/blob/e4c27bad7673a3dae733e279dbb6554eececd75e/frontend/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width
```


## License: MIT
https://github.com/persianpack/full-event-calendar/blob/9799660f3db5412224030fbf79303276e97061be/README.md

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width
```


## License: unknown
https://github.com/harish2704/harish2704.github.io/blob/c8e145dd64198845847e1f0d30a55fc9f700071b/online-group-vcf-file-generator/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;
```


## License: unknown
https://github.com/Alisson-Oliveira/rocketseat/blob/e13f51a37a56e8060034b2151006da94aae849e9/Eventos/next-level-week-esports/web/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=
```


## License: unknown
https://github.com/huangsihao7/scooter-WSVA/blob/e4c27bad7673a3dae733e279dbb6554eececd75e/frontend/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width
```


## License: MIT
https://github.com/persianpack/full-event-calendar/blob/9799660f3db5412224030fbf79303276e97061be/README.md

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width
```


## License: unknown
https://github.com/harish2704/harish2704.github.io/blob/c8e145dd64198845847e1f0d30a55fc9f700071b/online-group-vcf-file-generator/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;
```


## License: unknown
https://github.com/Alisson-Oliveira/rocketseat/blob/e13f51a37a56e8060034b2151006da94aae849e9/Eventos/next-level-week-esports/web/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=
```


## License: unknown
https://github.com/simonhlee97/simonhlee97.github.io/blob/4738b4f18f33721eb7e31c4ccb1bd9cdcef53897/test.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Poppins:wght@400
```


## License: unknown
https://github.com/harish2704/harish2704.github.io/blob/c8e145dd64198845847e1f0d30a55fc9f700071b/online-group-vcf-file-generator/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;
```


## License: MIT
https://github.com/persianpack/full-event-calendar/blob/9799660f3db5412224030fbf79303276e97061be/README.md

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;
```


## License: unknown
https://github.com/huangsihao7/scooter-WSVA/blob/e4c27bad7673a3dae733e279dbb6554eececd75e/frontend/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;
```


## License: unknown
https://github.com/Alisson-Oliveira/rocketseat/blob/e13f51a37a56e8060034b2151006da94aae849e9/Eventos/next-level-week-esports/web/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=
```


## License: unknown
https://github.com/simonhlee97/simonhlee97.github.io/blob/4738b4f18f33721eb7e31c4ccb1bd9cdcef53897/test.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Poppins:wght@400
```


## License: unknown
https://github.com/AtheerAPeter/zojie/blob/5b753daa930cee59b64b211a0305843eec60230f/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Poppins:wght@400;500;600;700;
```


## License: unknown
https://github.com/stardusteight-d4c/armazem/blob/0b219b91f756b6b6ba88f8533a4351377a704e5b/web/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Poppins:wght@400;500;600;700;800&family=Playfair+Display:wght@600;700;
```


## License: unknown
https://github.com/harish2704/harish2704.github.io/blob/c8e145dd64198845847e1f0d30a55fc9f700071b/online-group-vcf-file-generator/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;
```


## License: MIT
https://github.com/persianpack/full-event-calendar/blob/9799660f3db5412224030fbf79303276e97061be/README.md

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;
```


## License: unknown
https://github.com/huangsihao7/scooter-WSVA/blob/e4c27bad7673a3dae733e279dbb6554eececd75e/frontend/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;
```


## License: unknown
https://github.com/Alisson-Oliveira/rocketseat/blob/e13f51a37a56e8060034b2151006da94aae849e9/Eventos/next-level-week-esports/web/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=
```


## License: unknown
https://github.com/simonhlee97/simonhlee97.github.io/blob/4738b4f18f33721eb7e31c4ccb1bd9cdcef53897/test.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Poppins:wght@400
```


## License: unknown
https://github.com/AtheerAPeter/zojie/blob/5b753daa930cee59b64b211a0305843eec60230f/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Poppins:wght@400;500;600;700;
```


## License: unknown
https://github.com/stardusteight-d4c/armazem/blob/0b219b91f756b6b6ba88f8533a4351377a704e5b/web/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Poppins:wght@400;500;600;700;800&family=Playfair+Display:wght@600;700;
```


## License: unknown
https://github.com/harish2704/harish2704.github.io/blob/c8e145dd64198845847e1f0d30a55fc9f700071b/online-group-vcf-file-generator/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;
```


## License: MIT
https://github.com/persianpack/full-event-calendar/blob/9799660f3db5412224030fbf79303276e97061be/README.md

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;
```


## License: unknown
https://github.com/huangsihao7/scooter-WSVA/blob/e4c27bad7673a3dae733e279dbb6554eececd75e/frontend/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;
```


## License: unknown
https://github.com/Alisson-Oliveira/rocketseat/blob/e13f51a37a56e8060034b2151006da94aae849e9/Eventos/next-level-week-esports/web/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=
```


## License: unknown
https://github.com/simonhlee97/simonhlee97.github.io/blob/4738b4f18f33721eb7e31c4ccb1bd9cdcef53897/test.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Poppins:wght@400
```


## License: unknown
https://github.com/AtheerAPeter/zojie/blob/5b753daa930cee59b64b211a0305843eec60230f/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Poppins:wght@400;500;600;700;
```


## License: unknown
https://github.com/stardusteight-d4c/armazem/blob/0b219b91f756b6b6ba88f8533a4351377a704e5b/web/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Poppins:wght@400;500;600;700;800&family=Playfair+Display:wght@600;700;
```


## License: unknown
https://github.com/harish2704/harish2704.github.io/blob/c8e145dd64198845847e1f0d30a55fc9f700071b/online-group-vcf-file-generator/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=
```


## License: unknown
https://github.com/Alisson-Oliveira/rocketseat/blob/e13f51a37a56e8060034b2151006da94aae849e9/Eventos/next-level-week-esports/web/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=
```


## License: MIT
https://github.com/persianpack/full-event-calendar/blob/9799660f3db5412224030fbf79303276e97061be/README.md

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=
```


## License: unknown
https://github.com/huangsihao7/scooter-WSVA/blob/e4c27bad7673a3dae733e279dbb6554eececd75e/frontend/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=
```


## License: unknown
https://github.com/simonhlee97/simonhlee97.github.io/blob/4738b4f18f33721eb7e31c4ccb1bd9cdcef53897/test.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Poppins:wght@400
```


## License: unknown
https://github.com/AtheerAPeter/zojie/blob/5b753daa930cee59b64b211a0305843eec60230f/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Poppins:wght@400;500;600;700;
```


## License: unknown
https://github.com/stardusteight-d4c/armazem/blob/0b219b91f756b6b6ba88f8533a4351377a704e5b/web/index.html

```
Got it! I'll redesign all your pages and components using **Tailwind CSS classes only**. Let me update your pages with premium styling using Tailwind.

First, let's update **Home.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Home.jsx
import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import { products } from '../data/products';

const Home = () => {
  const [featuredProducts, setFeaturedProducts] = useState([]);
  const [heroVisible, setHeroVisible] = useState(false);

  useEffect(() => {
    setHeroVisible(true);
    setFeaturedProducts(products.slice(0, 8));
  }, []);

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 px-4 sm:px-6 lg:px-8 py-16 lg:py-32 bg-gradient-to-br from-slate-50 to-white border-b border-gray-200 ${heroVisible ? 'animate-fade-in-up' : ''}`}>
        <div className="flex flex-col justify-center animate-slide-in-left">
          <h1 className="font-playfair text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight">
            Premium Fashion Collection
          </h1>
          <p className="text-lg sm:text-xl text-gray-600 mb-8 leading-relaxed">
            Discover exclusive styles curated for the modern lifestyle
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Shop Now
            </Link>
            <Link 
              to="/products" 
              className="inline-flex items-center justify-center px-8 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1"
            >
              Explore Collection
            </Link>
          </div>
        </div>
        <div className="flex items-center justify-center animate-scale-in">
          <div className="w-full aspect-square bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg shadow-2xl"></div>
        </div>
      </section>

      {/* Categories Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-white">
        <div className="max-w-7xl mx-auto">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 text-center mb-16 animate-fade-in-up">
            Shop by Category
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {['Men', 'Women', 'Accessories', 'New Arrivals'].map((cat, idx) => (
              <div 
                key={idx} 
                className="group animate-fade-in-up hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2"
                style={{ animationDelay: `${idx * 0.1}s` }}
              >
                <div className="bg-gradient-to-br from-amber-400 to-amber-500 rounded-xl h-48 sm:h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins text-xl sm:text-2xl font-bold text-gray-900 mb-3">
                  {cat}
                </h3>
                <Link 
                  to="/products" 
                  className="text-teal-600 hover:text-amber-500 font-semibold inline-flex items-center gap-2 transition-colors duration-300 group-hover:translate-x-1"
                >
                  Explore →
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured Products Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
        <div className="max-w-7xl mx-auto">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-6 mb-16">
            <div>
              <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2 animate-fade-in-up">
                Featured Products
              </h2>
              <p className="text-lg text-gray-600 animate-fade-in-up" style={{ animationDelay: '0.1s' }}>
                Hand-picked items for you
              </p>
            </div>
            <Link 
              to="/products" 
              className="px-6 py-2 border-2 border-teal-600 text-teal-600 hover:bg-teal-600 hover:text-white font-semibold rounded-lg transition-all duration-300 whitespace-nowrap w-full sm:w-auto text-center"
            >
              View All
            </Link>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
            {featuredProducts.map((product, idx) => (
              <div 
                key={product.id} 
                className="animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter Section */}
      <section className="px-4 sm:px-6 lg:px-8 py-16 lg:py-24 bg-gradient-to-r from-teal-600 to-teal-700">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="font-playfair text-4xl sm:text-5xl font-bold text-white mb-4">
            Subscribe to Our Newsletter
          </h2>
          <p className="text-lg text-teal-100 mb-8">
            Get exclusive deals and latest updates delivered to your inbox
          </p>
          <form className="flex flex-col sm:flex-row gap-3">
            <input 
              type="email" 
              placeholder="Enter your email" 
              required
              className="flex-1 px-6 py-3 rounded-lg font-inter text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-amber-400 transition-all duration-300"
            />
            <button 
              type="submit" 
              className="px-8 py-3 bg-amber-400 hover:bg-amber-500 text-gray-900 font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg whitespace-nowrap"
            >
              Subscribe
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default Home;
````

Now update **productListing.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productListing.jsx
import React, { useState, useEffect } from 'react';
import { products } from '../data/products';
import ProductCard from '../components/ProductCard';
import Sidebar from './sidebar';

const ProductListing = () => {
  const [filteredProducts, setFilteredProducts] = useState(products);
  const [viewMode, setViewMode] = useState('grid');
  const [sortBy, setSortBy] = useState('newest');

  const handleSort = (value) => {
    setSortBy(value);
    let sorted = [...filteredProducts];
    
    switch(value) {
      case 'price-low':
        sorted.sort((a, b) => a.price - b.price);
        break;
      case 'price-high':
        sorted.sort((a, b) => b.price - a.price);
        break;
      case 'popular':
        sorted.sort((a, b) => b.rating - a.rating);
        break;
      default:
        break;
    }
    setFilteredProducts(sorted);
  };

  return (
    <div className="min-h-screen bg-slate-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Header */}
        <div className="mb-8 pb-6 border-b-2 border-gray-200 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Our Collection
          </h1>
          <p className="text-lg text-gray-600">
            {filteredProducts.length} Products Found
          </p>
        </div>

        {/* Controls */}
        <div className="bg-white rounded-xl p-4 sm:p-6 shadow-sm border border-gray-200 mb-8 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-wrap">
          <div className="flex items-center gap-4 w-full sm:w-auto">
            <label className="font-semibold text-gray-900 whitespace-nowrap">Sort by:</label>
            <select 
              value={sortBy} 
              onChange={(e) => handleSort(e.target.value)}
              className="px-4 py-2 border border-gray-300 rounded-lg font-inter bg-white cursor-pointer hover:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-0 transition-all duration-300"
            >
              <option value="newest">Newest</option>
              <option value="price-low">Price: Low to High</option>
              <option value="price-high">Price: High to Low</option>
              <option value="popular">Most Popular</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'grid' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('grid')}
            >
              ⊞ Grid
            </button>
            <button 
              className={`px-4 py-2 rounded-lg font-semibold transition-all duration-300 ${viewMode === 'list' ? 'bg-teal-600 text-white' : 'border border-gray-300 text-gray-900 hover:border-teal-600'}`}
              onClick={() => setViewMode('list')}
            >
              ☰ List
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="grid grid-cols-1 lg:grid-cols-4 gap-8">
          <Sidebar />
          <div className="lg:col-span-3">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
              {viewMode === 'grid' ? (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <ProductCard product={product} />
                    </div>
                  ))}
                </div>
              ) : (
                <div className="flex flex-col gap-4">
                  {filteredProducts.map((product, idx) => (
                    <div 
                      key={product.id} 
                      className="flex gap-4 p-4 border border-gray-200 rounded-lg hover:shadow-md transition-shadow duration-300 animate-fade-in-up"
                      style={{ animationDelay: `${idx * 0.03}s` }}
                    >
                      <div className="w-24 h-24 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                      <div className="flex-1">
                        <h3 className="font-poppins font-bold text-gray-900 mb-2">{product.name}</h3>
                        <p className="text-gray-600 text-sm mb-3">{product.description}</p>
                        <div className="flex justify-between items-center">
                          <span className="font-bold text-lg text-teal-600">${product.price}</span>
                          <button className="px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold transition-colors duration-300">
                            Add to Cart
                          </button>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductListing;
````

Update **Cart.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\Cart.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Cart = () => {
  const [cartItems, setCartItems] = useState([
    { id: 1, name: 'Premium T-Shirt', price: 45.99, quantity: 2, image: 'product1' },
    { id: 2, name: 'Casual Jeans', price: 89.99, quantity: 1, image: 'product2' },
  ]);

  const updateQuantity = (id, qty) => {
    if (qty > 0) {
      setCartItems(cartItems.map(item => 
        item.id === id ? { ...item, quantity: qty } : item
      ));
    }
  };

  const removeItem = (id) => {
    setCartItems(cartItems.filter(item => item.id !== id));
  };

  const subtotal = cartItems.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Shopping Cart
          </h1>
          <p className="text-lg text-gray-600">
            {cartItems.length} {cartItems.length === 1 ? 'item' : 'items'} in your cart
          </p>
        </div>

        {cartItems.length > 0 ? (
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {/* Cart Items */}
            <div className="lg:col-span-2">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                {cartItems.map((item, idx) => (
                  <div 
                    key={item.id}
                    className={`p-4 sm:p-6 flex gap-4 sm:gap-6 border-b border-gray-200 last:border-b-0 hover:bg-slate-50 transition-colors duration-300 animate-fade-in-up ${idx === 0 ? 'animate-delay-0' : ''}`}
                    style={{ animationDelay: `${idx * 0.05}s` }}
                  >
                    <div className="w-24 h-24 sm:w-32 sm:h-32 bg-gradient-to-br from-gray-300 to-gray-400 rounded-lg flex-shrink-0"></div>
                    
                    <div className="flex-1 flex flex-col justify-between">
                      <div>
                        <h3 className="font-poppins text-lg sm:text-xl font-bold text-gray-900 mb-2">
                          {item.name}
                        </h3>
                        <p className="text-gray-600 text-sm">SKU: {item.id}</p>
                      </div>
                      <div className="flex items-center gap-4">
                        <div className="flex items-center border border-gray-300 rounded-lg">
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity - 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            −
                          </button>
                          <span className="px-4 py-1 font-semibold">{item.quantity}</span>
                          <button 
                            onClick={() => updateQuantity(item.id, item.quantity + 1)}
                            className="px-3 py-1 text-gray-600 hover:bg-gray-100 transition-colors"
                          >
                            +
                          </button>
                        </div>
                        <button 
                          onClick={() => removeItem(item.id)}
                          className="text-red-500 hover:text-red-700 font-semibold transition-colors"
                        >
                          Remove
                        </button>
                      </div>
                    </div>

                    <div className="text-right flex flex-col justify-between items-end">
                      <span className="font-bold text-lg text-teal-600">
                        ${(item.price * item.quantity).toFixed(2)}
                      </span>
                      <p className="text-sm text-gray-600">
                        ${item.price.toFixed(2)} each
                      </p>
                    </div>
                  </div>
                ))}
              </div>

              <Link 
                to="/products" 
                className="mt-6 inline-flex items-center gap-2 text-teal-600 hover:text-teal-700 font-semibold transition-colors"
              >
                ← Continue Shopping
              </Link>
            </div>

            {/* Order Summary */}
            <div className="lg:col-span-1">
              <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 sticky top-24">
                <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                  Order Summary
                </h2>

                <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                  <div className="flex justify-between text-gray-600">
                    <span>Subtotal</span>
                    <span>${subtotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Tax (10%)</span>
                    <span>${tax.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between text-gray-600">
                    <span>Shipping</span>
                    <span className="text-green-600 font-semibold">Free</span>
                  </div>
                </div>

                <div className="flex justify-between items-center mb-8">
                  <span className="font-poppins text-xl font-bold text-gray-900">Total</span>
                  <span className="font-playfair text-3xl font-bold text-teal-600">
                    ${total.toFixed(2)}
                  </span>
                </div>

                <Link 
                  to="/checkout" 
                  className="w-full block text-center px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg mb-3"
                >
                  Proceed to Checkout
                </Link>

                <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300">
                  Continue Shopping
                </button>
              </div>
            </div>
          </div>
        ) : (
          <div className="bg-white rounded-xl p-12 text-center">
            <div className="text-6xl mb-4">🛒</div>
            <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-2">
              Your cart is empty
            </h2>
            <p className="text-gray-600 mb-6">
              Add some products to get started!
            </p>
            <Link 
              to="/products" 
              className="inline-block px-8 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
            >
              Start Shopping
            </Link>
          </div>
        )}
      </div>
    </div>
  );
};

export default Cart;
````

Update **productDetails.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\productDetails.jsx
import React, { useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { products } from '../data/products';

const ProductDetails = () => {
  const { id } = useParams();
  const product = products.find(p => p.id === parseInt(id)) || products[0];
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);

  const relatedProducts = products.filter(p => p.id !== product.id).slice(0, 4);

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Breadcrumb */}
        <div className="flex items-center gap-2 mb-8 text-sm text-gray-600 animate-fade-in-down">
          <Link to="/" className="hover:text-teal-600 transition-colors">Home</Link>
          <span>/</span>
          <Link to="/products" className="hover:text-teal-600 transition-colors">Products</Link>
          <span>/</span>
          <span className="text-gray-900 font-semibold">{product.name}</span>
        </div>

        {/* Product Details */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-16">
          {/* Images */}
          <div className="animate-fade-in-left">
            <div className="bg-gradient-to-br from-gray-200 to-gray-300 rounded-xl h-96 sm:h-[500px] mb-4 flex items-center justify-center overflow-hidden">
              <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400"></div>
            </div>
            <div className="grid grid-cols-4 gap-3">
              {[0, 1, 2, 3].map((idx) => (
                <button
                  key={idx}
                  onClick={() => setActiveImage(idx)}
                  className={`h-20 rounded-lg transition-all duration-300 ${
                    activeImage === idx 
                      ? 'ring-2 ring-teal-600 shadow-md' 
                      : 'opacity-60 hover:opacity-100'
                  } bg-gradient-to-br from-gray-200 to-gray-300`}
                />
              ))}
            </div>
          </div>

          {/* Details */}
          <div className="animate-fade-in-right flex flex-col justify-between">
            {/* Rating */}
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="flex text-amber-400">
                  {[...Array(5)].map((_, i) => (
                    <span key={i} className="text-xl">★</span>
                  ))}
                </div>
                <span className="text-gray-600 text-sm">(245 reviews)</span>
              </div>

              <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-3">
                {product.name}
              </h1>
              
              <p className="text-gray-600 text-lg mb-6">
                Premium quality product designed for everyday use
              </p>

              {/* Price */}
              <div className="flex items-baseline gap-3 mb-6">
                <span className="font-playfair text-4xl font-bold text-teal-600">
                  ${product.price}
                </span>
                <span className="text-lg text-gray-500 line-through">
                  ${(product.price * 1.2).toFixed(2)}
                </span>
                <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full font-semibold text-sm">
                  20% OFF
                </span>
              </div>

              {/* Stock Status */}
              <div className="flex items-center gap-2 mb-6">
                <div className="w-3 h-3 bg-green-500 rounded-full"></div>
                <span className="text-green-700 font-semibold">In Stock - Free Shipping</span>
              </div>

              {/* Description */}
              <div className="mb-8 pb-8 border-b border-gray-200">
                <h3 className="font-poppins text-lg font-bold text-gray-900 mb-3">
                  Description
                </h3>
                <p className="text-gray-600 leading-relaxed">
                  This premium product is crafted with the finest materials and attention to detail. Perfect for those who appreciate quality and style.
                </p>
              </div>

              {/* Add to Cart */}
              <div className="flex flex-col sm:flex-row gap-4 mb-8">
                <div className="flex items-center border border-gray-300 rounded-lg">
                  <button 
                    onClick={() => setQuantity(Math.max(1, quantity - 1))}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    −
                  </button>
                  <input 
                    type="number" 
                    value={quantity} 
                    onChange={(e) => setQuantity(Math.max(1, parseInt(e.target.value)))}
                    className="flex-1 text-center py-3 font-semibold border-l border-r border-gray-300 focus:outline-none"
                  />
                  <button 
                    onClick={() => setQuantity(quantity + 1)}
                    className="px-4 py-3 text-gray-600 hover:bg-gray-100 transition-colors font-semibold"
                  >
                    +
                  </button>
                </div>
                <button className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
                  Add to Cart
                </button>
              </div>

              <button className="w-full px-6 py-3 border-2 border-teal-600 text-teal-600 hover:bg-teal-50 font-semibold rounded-lg transition-colors duration-300 mb-6">
                ♡ Add to Wishlist
              </button>

              {/* Features */}
              <div className="space-y-3">
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Premium quality materials</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">Free shipping worldwide</span>
                </div>
                <div className="flex gap-3">
                  <span className="text-teal-600 font-bold text-xl">✓</span>
                  <span className="text-gray-600">30-day money-back guarantee</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Related Products */}
        <div className="border-t border-gray-200 pt-16">
          <h2 className="font-playfair text-4xl font-bold text-gray-900 mb-12">
            Related Products
          </h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {relatedProducts.map((prod, idx) => (
              <Link 
                key={prod.id}
                to={`/product/${prod.id}`}
                className="group animate-fade-in-up"
                style={{ animationDelay: `${idx * 0.05}s` }}
              >
                <div className="bg-gray-100 rounded-lg h-56 mb-4 group-hover:shadow-lg transition-shadow duration-300"></div>
                <h3 className="font-poppins font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
                  {prod.name}
                </h3>
                <p className="font-playfair text-xl font-bold text-teal-600">
                  ${prod.price}
                </p>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ProductDetails;
````

Update **checkout.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\pages\checkout.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
  const [formData, setFormData] = useState({
    firstName: '',
    lastName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [currentStep, setCurrentStep] = useState(1);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (currentStep < 3) {
      setCurrentStep(currentStep + 1);
    } else {
      setOrderPlaced(true);
    }
  };

  if (orderPlaced) {
    return (
      <div className="min-h-screen bg-slate-50 flex items-center justify-center px-4">
        <div className="bg-white rounded-xl shadow-lg p-8 sm:p-12 max-w-md w-full text-center animate-scale-in">
          <div className="text-6xl mb-4">✓</div>
          <h1 className="font-playfair text-4xl font-bold text-gray-900 mb-3">
            Order Confirmed!
          </h1>
          <p className="text-gray-600 mb-6 text-lg">
            Thank you for your purchase. Your order has been placed successfully.
          </p>
          <p className="text-gray-600 mb-8">
            Order ID: <span className="font-bold text-teal-600">#ORD123456</span>
          </p>
          <Link 
            to="/" 
            className="w-full inline-block px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Back to Home
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-slate-50 px-4 sm:px-6 lg:px-8 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8 animate-fade-in-down">
          <h1 className="font-playfair text-4xl sm:text-5xl font-bold text-gray-900 mb-2">
            Checkout
          </h1>
          <p className="text-lg text-gray-600">
            Complete your purchase securely
          </p>
        </div>

        {/* Progress Steps */}
        <div className="mb-12 flex items-center justify-between">
          {['Shipping', 'Payment', 'Review'].map((step, idx) => (
            <React.Fragment key={idx}>
              <div className={`flex flex-col items-center ${idx < currentStep ? 'opacity-100' : idx === currentStep - 1 ? 'opacity-100' : 'opacity-50'}`}>
                <div className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-lg mb-2 transition-all duration-300 ${
                  idx < currentStep 
                    ? 'bg-teal-600 text-white' 
                    : idx === currentStep - 1 
                    ? 'bg-teal-600 text-white border-2 border-teal-600' 
                    : 'bg-gray-200 text-gray-600'
                }`}>
                  {idx < currentStep ? '✓' : idx + 1}
                </div>
                <span className="text-sm font-semibold text-gray-900">{step}</span>
              </div>
              {idx < 2 && (
                <div className={`flex-1 h-1 mx-2 mb-6 transition-all duration-300 ${idx < currentStep - 1 ? 'bg-teal-600' : 'bg-gray-300'}`}></div>
              )}
            </React.Fragment>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Form */}
          <div className="lg:col-span-2">
            <form onSubmit={handleSubmit} className="space-y-6">
              {currentStep === 1 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Shipping Information
                  </h2>
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <input
                      type="text"
                      name="firstName"
                      placeholder="First Name"
                      value={formData.firstName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="lastName"
                      placeholder="Last Name"
                      value={formData.lastName}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="email"
                      name="email"
                      placeholder="Email"
                      value={formData.email}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="tel"
                      name="phone"
                      placeholder="Phone Number"
                      value={formData.phone}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="address"
                      placeholder="Street Address"
                      value={formData.address}
                      onChange={handleChange}
                      className="w-full sm:col-span-2 px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="city"
                      placeholder="City"
                      value={formData.city}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="state"
                      placeholder="State"
                      value={formData.state}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                    <input
                      type="text"
                      name="zipCode"
                      placeholder="Zip Code"
                      value={formData.zipCode}
                      onChange={handleChange}
                      className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                      required
                    />
                  </div>
                </div>
              )}

              {currentStep === 2 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Payment Information
                  </h2>
                  <div className="space-y-6">
                    <div>
                      <label className="block text-sm font-semibold text-gray-900 mb-2">
                        Card Number
                      </label>
                      <input
                        type="text"
                        name="cardNumber"
                        placeholder="1234 5678 9012 3456"
                        value={formData.cardNumber}
                        onChange={handleChange}
                        maxLength="19"
                        className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                        required
                      />
                    </div>
                    <div className="grid grid-cols-2 gap-6">
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          Expiry Date
                        </label>
                        <input
                          type="text"
                          name="expiryDate"
                          placeholder="MM/YY"
                          value={formData.expiryDate}
                          onChange={handleChange}
                          maxLength="5"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                      <div>
                        <label className="block text-sm font-semibold text-gray-900 mb-2">
                          CVV
                        </label>
                        <input
                          type="text"
                          name="cvv"
                          placeholder="123"
                          value={formData.cvv}
                          onChange={handleChange}
                          maxLength="3"
                          className="w-full px-4 py-3 border border-gray-300 rounded-lg font-inter focus:outline-none focus:ring-2 focus:ring-teal-500 transition-all duration-300"
                          required
                        />
                      </div>
                    </div>
                  </div>
                </div>
              )}

              {currentStep === 3 && (
                <div className="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 animate-fade-in-up">
                  <h2 className="font-poppins text-2xl font-bold text-gray-900 mb-6">
                    Review Order
                  </h2>
                  <div className="space-y-4 pb-6 border-b border-gray-200">
                    {['Premium T-Shirt (x2)', 'Casual Jeans (x1)'].map((item, idx) => (
                      <div key={idx} className="flex justify-between">
                        <span className="text-gray-600">{item}</span>
                        <span className="font-semibold text-gray-900">${idx === 0 ? '91.98' : '89.99'}</span>
                      </div>
                    ))}
                  </div>
                  <div className="mt-4 space-y-2">
                    <div className="flex justify-between text-gray-600">
                      <span>Subtotal</span>
                      <span>$181.97</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Tax</span>
                      <span>$18.20</span>
                    </div>
                    <div className="flex justify-between text-gray-600">
                      <span>Shipping</span>
                      <span className="text-green-600">Free</span>
                    </div>
                    <div className="flex justify-between items-center pt-4 border-t-2 border-gray-200 mt-4">
                      <span className="font-bold text-gray-900">Total</span>
                      <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
                    </div>
                  </div>
                </div>
              )}

              <div className="flex gap-4 flex-col-reverse sm:flex-row">
                <button
                  type="button"
                  onClick={() => setCurrentStep(Math.max(1, currentStep - 1))}
                  disabled={currentStep === 1}
                  className="flex-1 px-6 py-3 border-2 border-gray-300 text-gray-900 hover:border-teal-600 hover:text-teal-600 font-semibold rounded-lg transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
                >
                  ← Back
                </button>
                <button
                  type="submit"
                  className="flex-1 px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
                >
                  {currentStep === 3 ? 'Place Order' : 'Continue'}
                </button>
              </div>
            </form>
          </div>

          {/* Order Summary Sidebar */}
          <div className="lg:col-span-1">
            <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 sticky top-24 animate-slide-in-right">
              <h3 className="font-poppins text-xl font-bold text-gray-900 mb-6">
                Order Summary
              </h3>
              <div className="space-y-4 mb-6 pb-6 border-b border-gray-200">
                <div className="flex justify-between">
                  <span className="text-gray-600">Premium T-Shirt (x2)</span>
                  <span className="font-semibold">$91.98</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-gray-600">Casual Jeans (x1)</span>
                  <span className="font-semibold">$89.99</span>
                </div>
              </div>
              <div className="space-y-3 mb-6">
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Subtotal</span>
                  <span>$181.97</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Tax</span>
                  <span>$18.20</span>
                </div>
                <div className="flex justify-between text-sm text-gray-600">
                  <span>Shipping</span>
                  <span className="text-green-600">Free</span>
                </div>
              </div>
              <div className="pt-6 border-t-2 border-gray-200 flex justify-between items-center">
                <span className="font-bold text-gray-900">Total</span>
                <span className="font-playfair text-2xl font-bold text-teal-600">$200.17</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Checkout;
````

Now update **ProductCard.jsx** component with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\ProductCard.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const ProductCard = ({ product }) => {
  const [isWishlisted, setIsWishlisted] = useState(false);
  const [isHovered, setIsHovered] = useState(false);

  return (
    <Link 
      to={`/product/${product.id}`}
      className="group"
    >
      <div 
        className="relative bg-gray-100 rounded-xl h-48 sm:h-56 mb-4 overflow-hidden transition-all duration-300 transform hover:shadow-xl"
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
      >
        <div className="w-full h-full bg-gradient-to-br from-gray-300 to-gray-400 group-hover:scale-105 transition-transform duration-300"></div>

        {/* Badge */}
        <div className="absolute top-3 right-3">
          <span className="px-3 py-1 bg-red-500 text-white text-xs sm:text-sm font-bold rounded-full">
            -20%
          </span>
        </div>

        {/* Wishlist Button */}
        <button
          onClick={(e) => {
            e.preventDefault();
            setIsWishlisted(!isWishlisted);
          }}
          className="absolute top-3 left-3 w-10 h-10 rounded-full bg-white shadow-md flex items-center justify-center transition-all duration-300 transform hover:scale-110"
        >
          <span className={`text-lg transition-colors ${isWishlisted ? 'text-red-500' : 'text-gray-400'}`}>
            ♡
          </span>
        </button>

        {/* Quick View */}
        {isHovered && (
          <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center animate-fade-in-up">
            <button className="px-6 py-3 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1">
              Quick View
            </button>
          </div>
        )}
      </div>

      <div className="space-y-2">
        {/* Rating */}
        <div className="flex items-center gap-1">
          {[...Array(5)].map((_, i) => (
            <span key={i} className="text-xs sm:text-sm text-amber-400">★</span>
          ))}
          <span className="text-xs text-gray-600 ml-1">(48)</span>
        </div>

        {/* Name */}
        <h3 className="font-poppins font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-600 transition-colors line-clamp-2">
          {product.name}
        </h3>

        {/* Price */}
        <div className="flex items-center gap-2">
          <span className="font-playfair text-lg sm:text-xl font-bold text-teal-600">
            ${product.price}
          </span>
          <span className="text-xs sm:text-sm text-gray-500 line-through">
            ${(product.price * 1.2).toFixed(2)}
          </span>
        </div>

        {/* Add to Cart Button */}
        <button 
          onClick={(e) => {
            e.preventDefault();
            // Handle add to cart
          }}
          className="w-full mt-3 px-4 py-2 bg-gradient-to-r from-teal-600 to-teal-500 hover:from-teal-700 hover:to-teal-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg text-sm"
        >
          Add to Cart
        </button>
      </div>
    </Link>
  );
};

export default ProductCard;
````

Update **Navbar.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Navbar.jsx
import React, { useState } from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <Link to="/" className="flex items-center gap-2">
            <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
              <span className="text-white font-bold text-xl">✨</span>
            </div>
            <span className="font-playfair text-xl font-bold text-gray-900 hidden sm:inline">
              StyleHub
            </span>
          </Link>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8">
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Home
            </Link>
            <Link to="/products" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Products
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              About
            </Link>
            <Link to="/" className="text-gray-900 hover:text-teal-600 font-semibold transition-colors">
              Contact
            </Link>
          </div>

          {/* Icons */}
          <div className="flex items-center gap-4 sm:gap-6">
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors">
              <span className="text-2xl">🔍</span>
            </button>
            <button className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">♡</span>
              <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </button>
            <Link to="/cart" className="p-2 hover:bg-gray-100 rounded-lg transition-colors relative">
              <span className="text-2xl">🛒</span>
              <span className="absolute -top-1 -right-1 bg-teal-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                2
              </span>
            </Link>
            
            {/* Mobile Menu Button */}
            <button 
              onClick={() => setIsOpen(!isOpen)}
              className="md:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors"
            >
              <span className="text-2xl">☰</span>
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isOpen && (
          <div className="md:hidden border-t border-gray-200 py-4 space-y-3 animate-fade-in-down">
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Home
            </Link>
            <Link to="/products" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Products
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              About
            </Link>
            <Link to="/" className="block px-4 py-2 text-gray-900 hover:bg-gray-100 rounded-lg transition-colors">
              Contact
            </Link>
          </div>
        )}
      </div>
    </nav>
  );
};

export default Navbar;
````

Update **Footer.jsx** with Tailwind:

````jsx
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\src\components\Footer.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-gray-900 text-gray-300">
      {/* Main Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-16 border-b border-gray-800">
        <div className="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          {/* Brand */}
          <div className="lg:col-span-1">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-10 h-10 bg-gradient-to-br from-teal-600 to-teal-400 rounded-lg flex items-center justify-center">
                <span className="text-white font-bold">✨</span>
              </div>
              <span className="font-playfair text-xl font-bold text-white">StyleHub</span>
            </div>
            <p className="text-gray-400 text-sm leading-relaxed">
              Premium fashion for the modern lifestyle. Discover elegance in every piece.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Quick Links</h3>
            <ul className="space-y-2 text-sm">
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Home</Link></li>
              <li><Link to="/products" className="text-gray-400 hover:text-teal-400 transition-colors">Products</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">About Us</Link></li>
              <li><Link to="/" className="text-gray-400 hover:text-teal-400 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Customer Service */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Customer Service</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">FAQs</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Shipping Info</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Returns</a></li>
              <li><a href="#" className="text-gray-400 hover:text-teal-400 transition-colors">Size Guide</a></li>
            </ul>
          </div>

          {/* Follow Us */}
          <div>
            <h3 className="font-poppins font-bold text-white mb-4">Follow Us</h3>
            <div className="flex gap-4">
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>f</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>𝕏</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>📷</span>
              </a>
              <a href="#" className="w-10 h-10 bg-teal-600 hover:bg-teal-700 rounded-lg flex items-center justify-center transition-colors">
                <span>▶</span>
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Bottom Footer */}
      <div className="px-4 sm:px-6 lg:px-8 py-8">
        <div className="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 text-sm">
          <p className="text-gray-400">
            © 2024 StyleHub. All rights reserved.
          </p>
          <div className="flex gap-6 text-gray-400">
            <Link to="/" className="hover:text-teal-400 transition-colors">Privacy Policy</Link>
            <Link to="/" className="hover:text-teal-400 transition-colors">Terms of Service</Link>
          </div>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
````

Now update your **tailwind.config.js** to include custom fonts:

````js
// filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ['Inter', 'sans-serif'],
        poppins: ['Poppins', 'sans-serif'],
        playfair: ['Playfair Display', 'serif'],
      },
      animation: {
        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
        'fade-in-down': 'fadeInDown 0.6s ease-out forwards',
        'slide-in-left': 'slideInLeft 0.6s ease-out forwards',
        'slide-in-right': 'slideInRight 0.6s ease-out forwards',
        'scale-in': 'scaleIn 0.6s ease-out forwards',
        'pulse': 'pulse 2s ease-in-out infinite',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeInDown: {
          '0%': { opacity: '0', transform: 'translateY(-30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideInLeft: {
          '0%': { opacity: '0', transform: 'translateX(-30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        slideInRight: {
          '0%': { opacity: '0', transform: 'translateX(30px)' },
          '100%': { opacity: '1', transform: 'translateX(0)' },
        },
        scaleIn: {
          '0%': { opacity: '0', transform: 'scale(0.95)' },
          '100%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}
````

Finally, update your **index.html** to include Google Fonts:

````html
<!-- filepath: c:\Users\HP\OneDrive\Desktop\Full stack eccomerce\frontend\index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Poppins:wght@400;500;600;700;800&family=Playfair+Display:wght@600;700;
```

